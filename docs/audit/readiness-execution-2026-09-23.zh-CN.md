# 065未关闭事项：本批准备与集中验证结果

本轮按确认后的计划执行A0/A1，并遵循“先集中处理，再统一测试验收、最后总结”。用户明确暂不提供公开测试域名，因此本批完成可执行的夹具准备和前置验证，未启动完整90任务/100安全场景验收。**整体计划仍在进行中，不将本批准备完成写成全部验收通过。**

## 完成的处理

1. 新增第二版500条合成链接目录，包含HTTP存活、HEAD回退、重定向、身份验证、限流、404/410、503、超时及范围拒绝。同时导出实际收藏树、预期分类、原任务清单及T14/T29变更清单。原500条收藏和历史验收清单未被覆盖。
2. 增加一次性导出命令，拒绝覆盖旧目录；未配置域名时使用`.invalid`占位，填写域名后仍明确标记公开部署未核验。没有对外部署开发服务，也没有放宽浏览器本机地址访问限制。
3. 新增独立合成资料页面，保留密码、验证码、卡号控件及HttpOnly Cookie，不模拟品牌登录或付款，无提交行为。旧敏感页保留。新页面使用明确的localhost地址，保护正常开启，不绕过拦截页。
4. 完成下载范围、性能统计、旁白观察和平台前置的[集中验收清单](../acceptance/readiness-065.zh-CN.md)。产品代码未变，沿用065，没有升号、重新构建或安装App。

准备包：`.artifacts/open-items-20260923/fixture-package/`；脚本夹具版本7，新增链接目录版本2。包中所有浏览器执行标记保持false，实际前置结果单独记录。

## 集中验证结果

| 验证 | 结果与边界 |
| --- | --- |
| 夹具与证据脚本 | 12/12通过：500条分类、敏感控件保留、真实HTTP响应、导出正常/失败输入与拒绝覆盖、固定分母及证据约束 |
| 现有运行夹具自检 | 11/11通过；仅设施自测，不代表浏览器任务通过 |
| 仓库合同与差异格式 | 通过 |
| 新表单实际可达性 | 固定065 App正常显示所有合成控件；保护中心显示钓鱼防护和本地隐私处理仍开启 |
| 新表单模型前置验证 | 一次真实任务通过，只有page.observe/page.extract；两次模型请求，摘要只描述用途并给出来源。四种合成值在模型请求和响应中均零命中 |
| Cookie非空证明 | HTTP服务确实设置Cookie，退出后只读元数据确认localhost下具名Cookie存在且为HttpOnly；没有读取其加密值 |
| 旁白观察 | 字幕面板设置原本为开启；旁白实际开启后仍无法通过工具读取输出，返回timeoutReached。未计通过，已恢复关闭且未修改字幕偏好 |

模型前置原件：`model/model-diagnostic-readiness.jsonl`、`cua-receipts.json`与`verification-summary.json`。该一次结果不填入正式90位置，不回填057历史失败。

网络日志保留一条浏览器在任务开始前请求同来源`/favicon.ico`时自动携带合成Cookie的记录。这是正常同源Cookie传输，日志中的标记命中不能当作Agent外发；同时不能将“所有网络日志零命中”写进结论。页面读取与模型请求的身份、时间和原生工具分别保留。

## 前置条件刷新

| 项目 | 本次实查结果 | 当前处理 |
| --- | --- | --- |
| 公网链接夹具 | 用户选择暂不提供域名 | 已导出准备包，T14公开地址批次未启动 |
| Android | Pixel 9 Pro Fold已连接，Android17、arm64；安装包仍为151.0.7922.77，更新时间2026-09-05 | 纠正“设备未连接”旧快照；没有把旧包当065验收，也未覆盖手机资料 |
| 同内核基线 | 官方可用版本清单未提供153.0.8010.53；固定对照仍153.0.8010.47 | 严格对照尚需独立构建；本轮未执行该构建或重跑旧基线采样 |
| 存储 | 卷可用约98GiB；当前完整候选输出约37GiB | 记录预算输入，不假定新源码、工具链和编译峰值已满足，也未清理恢复点 |
| 发行凭据 | 仍无Developer ID Application；Release列表为空；runner清单403 | 未启动发行；403只说明当前权限不足 |
| 同事代码 | fetch成功，origin/main与同步分支仍为e6c17f7df7fd1a403493a2a0d3fb8a3e3093908a | 无新提交，未混入App |

## 恢复与剩余工作

固定App已正常退出。模型恢复为`http://127.0.0.1:8000/v1`、`gpt-4.1-mini`；旁白关闭、字幕设置未改，系统设置回到原外观页。本批49210夹具和57486诊断转发服务已停止，既有8000/18765服务保留。9项监控、15项研究、37条既有测试下载和完整收藏树前后完全一致。浏览器保留本次页面访问及具名合成Cookie记录，未宣称Profile逐字节未变。

下一步仍是：公开夹具就绪后冻结完整任务批次；同内核基线构建及配对性能测量；具备可观察输出的旁白验收；当前源码Android包和Windows设备验收；正式签名/公证/真实升级链。多文件权限在执行具名下载范围时处理，本轮没有发起下载或更改权限。

本轮未发现需要改065产品代码的新缺陷。新表单解决的是本地敏感页面不可达的测试前置；它不扩大为所有敏感站点已通过。待前置条件齐备后按原计划集中验收，普通新问题汇总后再由用户决定批量修复范围。

全部原件位于`.artifacts/open-items-20260923/`：`preparation-freeze.json`、`readiness-local.json`、`external-readiness.json`、`fixture-tests.log`、`runtime-selftest.json`、`verification-summary.json`、`profile-before.json`、`profile-after.json`、`restoration.json`、`ports-after.json`和`cua-receipts.json`。
