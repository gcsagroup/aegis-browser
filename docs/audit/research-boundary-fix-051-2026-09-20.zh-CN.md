# 051 研究比较空白容错修复与集中验收

日期：2026-09-20。**用户确认的 R-050-01 已修复，相关验收通过。** 固定测试入口已更新为 **Ver 2.0 (051)**：`/Users/lazy/Applications/GCSA Aegis Test.app`。

本轮只进行一次完整候选构建，耗时约 8.4 分钟；**520 项原生测试全部通过，7 项直接实机场景全部通过，另完成一次冷启动回读**。没有新增待补修问题，也没有扩大到047其余事项。

## 修改内容

比较前缀、值、后缀之间允许等价空白差异。例如网页“稳定指标为 42”，模型前缀“稳定指标为”加值“42”，现在可以正常核对。值的边界空白不会把同值拆成不同组。

只调整字段边界，内部文字仍须完整匹配；词和数字边界按实际原文位置检查。同一字段在页面重复出现时，每处都使用相同核验规则，不能挑选其中一个值而忽略冲突。沿用现有 ASCII 空白归一化，不新增语义近似匹配或单位换算。

没有修改收藏应用、研究保存、模型提示或界面流程。050 已通过的收藏实机证据继续保留，051 的完整原生回归覆盖既有收藏完成状态检查。

## 验证结果

| 场景 | 结果 | 原生界面和回执 |
|---|---|---|
| R1 重放050一致来源失败响应 | 通过 | 逐字重放原响应，三个42归一组并完成 |
| R2 重放050冲突来源失败响应并保存 | 通过 | 41／42／43各一，不指定多数；保存前显示待保存，真实保存后完成 |
| R3 正常模型三来源一致 | 通过 | 三个42归一组 |
| R4 正常模型三来源冲突 | 通过 | 保留三个值及对应来源，不指定多数 |
| R5 正常模型十来源并保存 | 通过 | 42为8个来源；来源5为43，来源8为41；来源10正确进入42组 |
| R6 注入错误分类正文 | 通过 | 注入“7个一致、来源10为例外”，浏览器仍生成8／1／1 |
| R7 注入伪造数值99999 | 通过 | 拒绝伪造值，显示部分完成，保留真实42原文 |
| R8 冷启动回读（补充检查） | 通过 | 两条新研究的分组、数值和3／10个来源完整保留 |

19 项研究定向测试先通过，再冻结源码完成一次浏览器构建。完整原生回归为 Core 242、Browser 235、更新器18、Actor 25，共520项，无失败、无重试。新增用例覆盖原失败数据、空格／制表／换行边界、同值合并、英文词边界、142与42、18.5与18、完整单位、1 2与12、否定词和同页冲突；既有缺失、重复、换页、截断来源回归同样通过。

实机使用固定App、独立测试资料和原本机模型。R1/R2为存档响应重放，R6/R7为明确记录的故障注入；R3/R4/R5为原模型正常返回。重放参数与050存档逐字核对，不把测试中继的替换行为当作产品能力。已检查原生界面与截图。

## 资料与恢复

- 固定App签名及安装后文件树校验通过；Bundle ID为 `app.gcsa.aegis`，Team为 `HX69X22ZGL`。
- 原有13条研究、8条暂停监控及收藏树保持不变。保留两条标有“051”的新研究供复核，现共15条。
- 原模型地址已通过界面恢复为 `http://127.0.0.1:8000/v1`，服务仍可用；本轮两个临时服务已退出，测试App正常退出。
- 050 App及匹配资料备份保留在 `.artifacts/integration-20260920/install-2.0.0.51/previous-installed.app`、`profile-before-051`。恢复时须先退出App，并将App和对应资料成对恢复。

## 身份与证据

- 候选源码：`c37546758d88769d744bedaee4a4925676db9310`；树：`62c100066529e1cae6edf70cc0e9b651077d88fa`。
- Chromium：153.0.8010.53；产品：2.0.0.51；192个Chromium补丁、3个V8补丁。新增0192补丁回放树与源码一致。
- 构建清单 SHA-256：`bfacb0cd9f928085e2bbbd12fa152b88d141178dba191353203bee28028ac18e`。
- 固定App文件树 SHA-256：`0e19454343983bfe70ce2e5ec75feec84801dad86f37320003eaa4e6325a03b3`。
- 证据目录：`.artifacts/integration-20260920/candidate-051/benchmark-051/`；主要文件为 `runs.json`、`cua-receipts.json`、`fault-and-replay-validation.json`、`reviewed-summary.json`、`build-and-native-tests.json`、`final-consistency-check.json`。
- 改动及补丁回放：`.artifacts/integration-20260920/repair-051/`；完整候选快照：`/Users/lazy/Projects/GCSA-aegis-build/upstream-candidate/integration-051/product`。

结论仅覆盖本次批准的修复。050的原始失败记录保留；未重跑全量90／100场景，未将候选提升为正式发行，也不表示不同措辞、单位或测量条件在语义上等价。047其余待确认事项继续见[集中验收问题文档](integration-047-issues-and-decisions-2026-09-20.zh-CN.md)。
