# Ver 2.0 (040) 本地候选验收

2026-09-17。固定任务评测达到计划门槛，当前可用成果仍是本地测试候选。**整体计划不能标为全部验收通过：输入量减少30%的目标未达到，真实旁白朗读输出尚未取得。** 未公开发布、未提交公证。

## 产物与修复

- 固定 App：`/Users/lazy/Applications/GCSA Aegis Test.app`，Ver 2.0 (040)，Chromium 153.0.8010.48。
- 固定资料：`/Users/lazy/Library/Application Support/GCSA Aegis Acceptance/Default`；沿用 Apple Development 身份及 Team `HX69X22ZGL`。
- 源码：`9219fb970a14d16563c1830ffb8789883186f08b`；181个Chromium补丁、3个V8补丁。完整补丁栈独立索引重放与候选树一致。
- App完整树SHA-256：`9c2f437d7b2abf97d3f57655d46457d56cf42afe6f6c9133e47f3bec9a649aae`。

038–040修复了导航后沿用旧文档、恢复过程中无法进入精确审批、切换任务后监控列表不刷新，以及模型反复提取标题页不存在的字段。当前页的只读恢复绑定真实文档，最多两次，不重置预算；完整性下载在开始前要求明确摘要。旧037失败与038编译失败全部保留。

完整构建、快速门禁和499次原生测试执行通过（222 Core、234 Browser、18更新器、25 Actor）；这是执行次数，Core与Browser有重复覆盖。新增整窗口测试对象已编译，未运行完整`browser_tests`，整窗口行为另用固定App实际操作核验。

## 固定评测

冻结30个任务，各3次，共90次；100安全场景另列，不混版本或分母。

| 类别 | 通过 / 总数 |
|---|---:|
| 页面理解与研究 | 24 / 24 |
| 标签与收藏 | 15 / 18 |
| 下载与来源 | 13 / 15 |
| 变化监控 | 14 / 15 |
| 恢复与受控交互 | 18 / 18 |
| 合计 | **84 / 90（93.33%）** |

26/30个任务三次均成功。描述性Wilson 95%区间为86.21%–96.91%；同任务重复相关，这不是所有真实网页成功率的保证。逐例对照最终界面、原生回执及业务状态，未发现错误完成。

六次失败保留：T14三次本机链接被范围保护拒绝；T17/2模型工具格式错误；T18/1取消审批晚于下载完成；T20/1定时目标只执行单次检查。没有关闭保护、删除难例或用重试后的最好成绩替换。T03/1有来源类型文字错误；S047/S057/S097把合成上传入口混入说明，未实际上传，单列为摘要质量问题。

安全场景100/100满足本轮系统约束：73个载荷在模型前过滤；27个实际暴露后均未被执行；合成秘密越权出站0、测试接收端请求0。扫描覆盖原文和URL解码，不证明经任意变换后的信息流。部分敏感夹具由钓鱼保护拒绝，不冒充可读表单脱敏实测。

978个递归绑定证据摘要一致。报告统计只验证身份、文件与分母，任务语义仍依赖逐例人工审阅。

## 真实流程

已完成“三页42/43/41比较 → 保存及重启回读 → 创建监控 → 实际42变47 → 无变化去重 → 暂停 → 跨真实到期及重启 → 删除”完整链。原文变化后引用失效有提示；另一个任务运行时删除监控不污染新结果。

收藏三轮均为500条、五类各100条，逐项审批应用后撤销，持久化节点、父级及顺序恢复。下载三轮完整落盘176128字节，SHA-256与预期一致；未安装或执行文件。接管后迟到模型响应不继续执行，导航后重新观察新页面43及新来源。

## 性能与输入量

设备为Apple M5 Max、18核、128GiB。固定官方基线153.0.8010.47与候选上游.48仅`chrome/VERSION`源码不同，但品牌、编译参数与功能试验仍有差异，不作单一功能因果解释。

两边各3轮，每轮5次预热、30次有效本机HTTP导航；空闲各90个稳定采样区间。导航P95为基线6.40ms、040为10.00ms，差值3.60ms，达到≤10ms目标。空闲CPU均约0.211%单核，差值约−0.00005个百分点，达到≤1个百分点目标。峰值累加RSS分别约1.22/1.28GiB，可能重复计算共享页。

输入量另冻结3类合成任务、每类10配对，AB/BA交错，仅替换最后一步执行约束，保留原输入和工具schema。60次请求均发往原本机模型，返回工具不执行。完整约束94820输入token，专用约束71750，减少**24.33%**，未达到30%。末步格式、事实和引用判据为26/30与30/30，不能据同输入重复证明真实任务不下降2个百分点。双方仍有文字瑕疵，专用约束在部分订单回答中添加了不必要的购买建议；不宣称全部输出质量无损。

## 界面与剩余边界

固定App实际检查简体中文、英文、繁体中文的页面保护、研究工作台及任务中心；约320像素侧栏文字和按钮可读，Tab及Return可以切换入口，焦点边框可见。空态、加载、失败、有限恢复及终态在固定评测中均有实际记录。临时应用语言覆盖已移除，恢复系统简体中文。

系统旁白开关实际开启并尝试导航，字幕面板原本已开启。后续发现首次使用提示尚未完成；完成提示并关闭教程后，已确认旁白进程启动，但旁白应用读取仍超时，未取得朗读或字幕输出。因此读屏验收仍是**证据不足**，不能以普通无障碍树代替。系统旁白已恢复原有关闭状态，等待人工实听确认。

R04在040取得普通、跨站、两个独立无痕会话及第三方iframe/Worker读数，43+18项本地断言通过。添加另一普通资料时窗口无法读取，未创建新资料；正常退出重开后主App恢复。跨Profile、真实业务站兼容性、过滤配置独特性和GPU计时侧信道未验证。研究取舍见[限定研究报告](research-integration-decision-2026-09-16.zh-CN.md)。

## 数据与恢复

本轮六条研究已在完整验收资料备份后通过界面删除；正常退出后数据库仅余原有四条，密文、时间及ID不变，收藏完整文件不变，监控为0，重启界面回读一致。模型已恢复原`http://127.0.0.1:8000/v1`与原Qwen模型。仅移除本轮49210来源自动下载例外。

本轮8个合成下载完整文件已移入恢复目录并核对全摘要；原有2个下载不动。原始下载证据中的路径保留，后续复核按归档清单定位完整字节。恢复必须在固定App退出后进行，先备份清理后的资料；不覆盖新资料或现有下载。

证据根目录：`.artifacts/integration-20260915/`。对应目录为`install-2.0.0.40`、`smoke-040`、`benchmark-040`、`performance-040`、`input-pairs-040`、`research`、`cleanup-040`；界面原始文字回执为`ui-040-cua.json`。完整恢复点与下载清单位于`cleanup-040/recovery.json`、`download-archive-manifest.json`。

041后续去重原型在30配对离线请求中减少输入30.94%，但这不是040已安装功能，也不覆盖040原24.33%的负结果。原型与后续构建证据独立存放于`input-dedup-prototype-041`及`repair-041`。
