# 050 收藏与研究比较修复验收

> 后续状态：用户确认后，R-050-01 已在051修复并通过相关验收，见[051记录](research-boundary-fix-051-2026-09-20.zh-CN.md)。下文保留050当时的失败和待确认结论。

日期：2026-09-20。**收藏修复验收通过；研究错误归类已修正，但正常比较的空白容错问题使该项完整验收尚未通过。** 按用户要求，本批集中验收结束后记录问题，等待确认补修范围，不继续改版。

固定测试入口已更新为 **Ver 2.0 (050)**：`/Users/lazy/Applications/GCSA Aegis Test.app`。完整构建和 **515 项原生测试全部通过**；9 项直接实机场景中 **7 通过、2 未通过**。另完成两条研究的保存、冷启动回读和资料一致性核对；这些补充检查不抵消两个失败。

## 已完成的修改

- 收藏完成状态复用规划器的收藏、预览和否定意图判断。应用目标必须有本任务有效写入回执及回读快照；只预览、旧任务回执、失败、无有效撤销凭证或已经撤销，都不能据此显示应用完成。写入仍须精确批准，撤销目标不会被当作新的应用目标。
- 研究完成响应增加有界的比较字段、共同原文上下文和逐来源值。浏览器核对所选页面的完整观察，自行生成同值分组、数量和相对多数来源的差异；不使用模型自由正文中的分类结论。
- 重复、缺失、换页、截断、截取部分数值或同页冲突会保守处理。没有超过半数的同值组时不指定多数值；缺失值不列为例外。
- 保存沿用已有加密存储和真实回执。比较没有完成时，保存和重启都不会把它变成完成。

## 集中实机结果

| 场景 | 结果 | 实际证据 |
|---|---|---|
| B1 合法预览 | 通过 | 500 条预览显示完成，明确尚未应用；收藏树未改变 |
| B2 未应用但模型声称完成 | 通过 | 强制模型只规划预览并声称完成；界面显示部分完成和缺少写入回执，树未改变 |
| B3 精确批准后应用 | 通过 | 批准绑定具体方案和快照；实际 500 条收藏的父级改变，界面显示已应用 |
| B4 撤销 | 通过 | 原生撤销成功；冷退出后逐节点核对编号、父级、顺序、名称、链接与 GUID，恢复原树 |
| R1 三个一致来源 | **未通过** | 模型值均为42，但比较前缀少一个空格，正确结果被降为部分完成 |
| R2 三个冲突来源并保存 | **未通过** | 同一空格问题导致比较回退；42／43／41 原文和引用仍保留，保存与冷启动部分状态回读通过 |
| R3 十来源正常比较 | 通过 | 42 的组为8个来源，来源5为43、来源8为41；来源10正确进入42组 |
| R4 十来源错误正文重放 | 通过 | 注入“7个一致、来源10为例外”的错误正文；原生结果仍为正确的8／1／1分组 |
| R5 伪造结构化数值 | 通过 | 在原本可匹配的字段中注入99999，界面只保留真实原文并显示部分完成，没有展示伪造值 |

R4 的正确比较另外保存并冷启动回读，分组与十个来源均保留。R2 的部分比较保存后仍有未完成说明。原生界面和截图已检查；这不等于重新完成全量90项任务、安全100项或多语言人工验收。

## 待确认：R-050-01 原文边界空白容错

**触发条件：** 网页原文是“稳定指标为␠42”，模型提交的前缀是“稳定指标为”，值是“42”。这里的 `␠` 仅用于显示一个真实空格。当前代码保留前缀边缘空格并逐字匹配，因此少一个空格就拒绝该字段。

**影响：** 本轮 R1、R2 的原始事实正确，却回退为原文摘录和部分完成。该问题没有放行错误分类，但降低了正常比较的可用性，因此不能宣布研究项整体完成。

**建议下一批只补这一点：** 在字段前缀、值、后缀的边界允许等价空白差异，仍要求来源、上下文、完整值与单位一致。不能简单删除所有空白，不能让 `142` 匹配 `42`、让 `1 2` 匹配 `12`，也不能改写否定词或合并不同单位。

**完成标准：** 重放 R1、R2 原始响应后正常完成；十来源仍为8／1／1；伪造值、部分数字、不同单位、重复及缺失来源仍受阻；保存回读保持结果。集中实现后只做必要原生回归、一次候选构建和相关实机验收，不重跑全量90／100场景。

**状态：等待用户确认，尚未实施。** 不将其他047体验问题扩入本批。

## 构建与资料

- 050 源码：`31f5ae181e2d17b7c64e9ad512f53504bb1adbb5`；Chromium `153.0.8010.53`；候选191个 Chromium 补丁及3个 V8 补丁。
- 清单 SHA-256：`79363c0cf6ae65663b73c2aac936574dd99280abfc028c77ddc39e913fca9896`。
- 固定 App 树 SHA-256：`56b31f620a7eeaae80f18cc65b039c17294807936a0f10bfaa773fe1efed9114`；稳定测试签名和独立资料路径沿用原配置。
- 原生测试：核心237、浏览器235、更新器18、Actor25，共515。夹具修正后先跑的17项定向回归全部通过。本批快速检查通过，日志保留在 `repair-048/quality-fast-root.log`，未重复运行未受影响的前端检查。
- 构建经历如实保留：048 在静态复查发现撤销意图边界后停止；049 完整构建成功，但新增测试使用空权限范围及十来源八标签预算触发构造检查；050 修正这些测试夹具并全部通过。048、049 都没有安装，生产逻辑在049后未再更改。没有把失败构建或夹具崩溃记录删除。
- 安装前保留047与完整资料备份：`.artifacts/integration-20260920/install-2.0.0.50/`。
- 原有11条研究密文和8条暂停监控逐字段一致，500条收藏恢复原树。本轮两条研究保留供复核，目前共13条研究；所有权编号见 `owned-research.json`，完整数据库备份见 `before-cleanup.sqlite`。
- 原模型 `http://127.0.0.1:8000/v1` 已恢复并验证可用；两个本轮测试服务已核对退出，App已正常退出。

本轮证据目录：`.artifacts/integration-20260920/candidate-050/benchmark-050/`。`runs.json` 对应模型往返和原生回执，`cua-receipts.json` 为实际界面记录，`evidence-manifest.json` 绑定哈希，`reviewed-summary.json` 保留7通过2失败，`final-consistency-check.json` 核对身份与收尾。047原失败也单独保留在 `original-047-failures.json`。

050仍是带已知研究回退问题的隔离测试候选，没有提升根正式入口或发布。原文比较核对的是字段、来源、分组与数量的一致性，不证明来源本身真实，也不自动证明不同措辞或测量条件等价。
