# Ver 2.0 (023) 本地候选记录

2026-09-16。**完整构建、产物身份绑定、快速门禁和 462 项原生测试通过；已沿固定路径安装并进行实机验收，整体计划未完成。** 固定 App 为 Ver 2.0 (023)。本记录不代表公开发行、公证或跨平台验收。

## 本次交付

- 研究结果可显式加密保存，保留来源、摘录与正文摘要；普通资料最多保存 20 条，不自动删除既有存档。资料库 8→9 迁移、删除、损坏密文拒绝、重启读取和无痕隔离都有原生覆盖。无痕只保留会话内存。
- 保存来源可打开原文片段，并由浏览器重新读取、比较正文摘要，显示匹配、变化或无法核对。该比较不调用模型，也不等同于事实核验。
- 部分来源读取重试耗尽时，只保留实际读到的摘录和明确的未完成项；作用域失效、旧文档、取消、预算耗尽不能借此继续。正文截断不能支持完整研究结论，全部不可读仍失败。
- 下载证据展示原生地址、文件名、字节、摘要和核验状态。多次下载不能混用不同文件的证据；失败仍保留实际回执。发布者、仓库、版本、签名没有独立验证器时明确未核验。证据仅有界保存在会话内，不宣称跨重启持久的签名证明。
- 页面保护把当前页事件与未归属当前页的站点历史分开，避免把另一页的拦截计入本页。指纹诊断不再通过品牌字符串判断防护效果。
- 研究与下载界面补齐三语文本；真实界面函数测试覆盖选择范围、惰性文本、失败刷新、引用变化、未知状态和下载空态。固定 App 的窄侧栏、键盘与读屏检查仍未完成。

## 身份与验证

候选源码：`b6afd34ba5ba9478f21b656c0b5b0a6f1c9bb4d8`。源码树：`891ffb87cee68e2bd682c2f7b74eb0d7573f5013`。

Chromium：`153.0.8010.48`，164 个 Chromium 补丁和 3 个 V8 补丁。163 为 023 版本预备补丁，164 在首次 023 编译前补入测试前置步骤和截断证据约束；未改写历史补丁。增量补丁重放与对应源码树一致。

构建清单 SHA-256：`014d467a2ff9942b9ef116b45e00018a7471fdbb60fa016fd8d63c8761d041c9`，资格为本机诊断身份绑定。构建产物及固定测试身份重签通过；2026-09-16 18:40完成018包和匹配资料备份后，沿原路径安装023，签名和资料前后核对一致。

| 检查 | 结果 |
|---|---|
| quality:fast | 退出 0 |
| GN、相关 WebUI 类型与 lint、设置/历史测试类型检查 | 通过 |
| 完整浏览器与测试程序构建、产物版本核对、身份绑定 | 通过 |
| Agent Core | 196/196，单次通过 |
| 浏览器服务与结果核验 | 223/223，单次通过 |
| 更新器 | 18/18，单次通过 |
| Actor 地址权限回归 | 25/25，单次通过 |
| 固定 App 新功能实机闭环 | 研究保存、来源回查、无痕清除与事件归属通过；监控继续验收，发现两类下载问题和摘录缺失 |

原始证据：`.artifacts/integration-20260915/m3/input-023/`、`m3/quality-023.log`、`m3/patch-replay-023-final.json`，以及候选目录 `validation/*2-0-0-23*`。原生测试先列名再核对单次结果，没有通过重试挑选最好结果。

## 保留的失败

019 的两项 Core 失败/崩溃记录保留，020 修复后全组通过。021 因下载代码插入了错误回调而编译失败，196 项 Core 补充通过，未安装。022 的完整构建、身份绑定与 196 Core 通过，浏览器组 222/223 通过；新增下载测试缺少产品强制的前置步骤而失败。023 补齐测试计划，保留原断言，462 项原生测试全部通过。

## 固定 App 实测与剩余工作

固定路径为 `/Users/lazy/Applications/GCSA Aegis Test.app`，资料为 `/Users/lazy/Library/Application Support/GCSA Aegis Acceptance`。018 在打开添加个人资料入口后，操作工具只能读取“指纹隔离研究测量”窗口标题，无法取得浏览器控件。重建工具会话、正常退出和访达重新打开均未恢复；访达自身控件可读。主线程采样不足以证明 App 死锁，原因仍未确定。没有强杀进程、修改系统权限或替换运行中的 App。

用户回复“已退出”后，已核实进程结束，备份018及匹配资料，完成023清单与稳定签名核对并沿原路径安装。CUA控件恢复可读，实际回读产品023、内核153.0.8010.48及Acceptance/Default资料。旧App和资料备份位于 `.artifacts/integration-20260915/install-2.0.0.23/`。回退必须恢复匹配资料。

023实测：

- 三页研究正确列出42/43/41与三个引用，明确原文未提供日期；显式保存后列表1/20。
- 原文核对依次得到一致、改成47后变化、恢复42后一致；切换另一URL则无法核对，未增加模型调用。
- 无痕列表初始0/20，保存后1/20，关闭全部无痕窗口再打开回到0/20；普通列表仍为1/20。
- 另一页钓鱼拦截后，正常来源页本页处理数为0，站点历史单列；未绕过拦截。
- 失败来源在首位或末位均报告部分结果，全失败不生成结果或保存入口。但部分摘录遗漏独立行内数字，需修复后复验。
- 来源预填监控后按钮不刷新，编辑文字才可创建；024修复并通过完整构建与462项原生测试，未安装。
- 普通下载命令被错误降为只读研究；明确官方入口虽保留下载工具，模型仍自造提取字段而失败。025修复意图识别及下载提取选项，通过完整构建和465项原生测试，未安装。
- 本地15分钟监控已创建，19:06实际检查成功且未显示变化通知；随后夹具改47，等待下一轮变化检查。

原始进度证据为 `.artifacts/integration-20260915/m3/native-023-progress.json` 及本轮CUA操作记录。研究测量另有023第三方iframe与三类Worker的三次可见操作，18项稳定性/分区断言通过，未替代跨Profile与业务兼容性。

剩余验收包括：修复候选复验、监控变化与去重/暂停/取消、正常重启保存、下载证据、三语/窄侧栏/键盘，以及冻结的90次任务、100个安全场景和性能对照。原生测试与上述重点实测不能代替完整评测。

研究取舍与现有实验范围见[论文整合取舍](research-integration-decision-2026-09-16.zh-CN.md)。R01/R02/R03 按计划以证据不足结束本轮材料预检；R04/R05 仍有未验收项，不宣称论文复现或生产能力。
