# 046 实机回归及研究保存状态一致性修复

> 后续状态：047 已安装并完成本轮集中验收，详见[047问题与决策文档](integration-047-issues-and-decisions-2026-09-20.zh-CN.md)。下文保留046当时记录，不代表当前运行状态。

2026-09-20。**046 的数字变化修复已连续三轮实机通过，完整构建及 505 项原生测试通过；随后发现研究实际保存后仍显示“未保存”的状态矛盾，已修复为 047，完整构建和 506 项原生测试通过，等待固定浏览器空闲后安装实测。整体计划尚未完成。**

## 身份及已验证部分

固定 App `/Users/lazy/Applications/GCSA Aegis Test.app` 的关于页显示 `Ver 2.0 (046)`、Chromium `153.0.8010.53`。独立资料、稳定 Apple Development 签名保持；替换前后逐文件核对资料一致。

- 源码：`fa9dfac6f9967a0725cd579082fd957df75e1468`，187 个 Chromium 补丁及 3 个 V8 补丁。
- 构建清单：`88f2ecc269fe28cfc28fb1c977dadb1d310f5f3a99589cf78c36f017e544ed02`。
- 固定 App 树 SHA-256：`b8269e6bafc7f634c18149074a3925dfb258b94834cc3b13c30e451f97752fdf`。
- 原生测试：`227 + 235 + 18 + 25 = 505`，实际全部通过。
- 完整构建、身份绑定、严格签名校验、`quality:fast` 均通过。

三轮监控创建、相同正文复查、真实 42→47 变化共 9 次通过。纯数字摘要三次都显示“移除42、新增47；具体含义尚未判定”，模型日志确认没有该次摘要调用。每个新增监控均暂停，保留跨真实到期及重启删除的后续验收；这两项尚未算通过。

单页总结、三来源分歧比较、订单核对各 3 次共 9 次通过；核对 42／43／41、原始来源与 113 CNY 订单明细，未点击最终购买。T03 第三轮有“一至样本”同音错字，原输出保留，未改写为更好结果。

## 输入量与协议偏差

9 次任务的实际全链输入为 **34,867 tokens**，包括 T01 第一轮的额外模型调用；比 041 同组历史实际 45,252 减少 **22.95%**，比旧完整约束参考 54,033 减少 **35.47%**。这是两个不同口径，旧参考含历史前缀加恢复约束重放，并非本轮同步端到端对照；不能用其中一个替代另一个，也不能用 9 次证明成功率下降不超过 2 个百分点。

一次 T03 操作误用了“比较这些来源，明确保留冲突结论。”，与冻结提示不一致，已单列原始回执并排除正式统计。之后实际使用冻结原文“比较来源 1、5、8，不要替我消除分歧。”执行三次；归档器验证每次模型请求的原始目标，不靠重试挑选分数。

## 保存缺陷及证据边界

T08 前两轮比较后显示原生待保存，点击后显示已完成和保存回执。第三轮模型合法返回 `partial`，正文最后写“保存状态：未保存。”，未完成项写“把结果和引用保存到研究项目”。点击保存后，真实加密存储成功，但这两段模型文字没有随原生回执更新，界面同时显示已保存与部分完成，记为失败。

正常退出后，数据库只读快照确认新增 3 条研究，原 8 条密文未改。重启回读期间界面工具检测到用户操作，任务重新读取状态后暂停界面动作，因此前两轮的完整重启回读仍为证据不足，不能仅凭新增密文和保存回执算通过。

046 共执行 21 次正式任务，目前归档 **18 通过、1 失败、2 证据不足**；安全场景尚未执行。另有一项提示偏离协议记录。全部归档引用文件已重新计算 SHA-256 一致。原件在 `.artifacts/integration-20260920/candidate-046/benchmark-046/`，重点为 `input.json`、`halted-for-repair.json`、`T08-3-cua.json`、`T08-3-model.json`、`research-owned-records.json`、`input-cohort-summary.json`。

## 047 修复与剩余工作

047 对明确的保存目标，将已观察到的独立状态行和有限白名单存储待办规范为浏览器自己的待保存项。比较正文和来源保留，未知表述、读取失败、缺少来源等未完成事项不按包含“保存”二字就删除；保存失败仍部分完成，只有真实存储成功回执才清掉原生待办。模型提示同时要求仅报告来源读取和比较状态，不自行报告存储状态。

源码 `41ebf6d24b0b19e42e19aabfa9f17881db7469d5`，188 个 Chromium 补丁，独立重放树一致。新增单测覆盖观察到的中英文及繁体存储待办、失败/重复呈现、成功回执、其他未完成事项保留、未知复合事项和“不要保存”目标；`quality:fast` 通过，完整构建与 `228 + 235 + 18 + 25 = 506` 项原生测试实际全部通过。构建清单 SHA-256 为 `627f542c4c47ac7fa74ce6e4b4ddb4665965097f0db90a19edbc8fa273ef24e5`。另准备定向重放 046 实际错误模型响应，合成中继的未授权、错误目标、注入和恢复自测通过，尚不算浏览器验收。

当前固定 App 仍为 046，用户正在使用；047 未安装。现有 8 个暂停监控及 11 条研究按轮次单列所有权，其中原始基线 4 条研究必须保留。模型配置暂仍为 046 本机诊断中继 `http://127.0.0.1:51985/v1`，转发到原 8000 模型；尚未恢复或停止 046 服务，以免在用户使用期间改变设置。

047 的安装、定向回归、完整任务与安全评测、三语及读屏、性能边界说明、资料恢复和根补丁/pin 收敛仍待完成。不会把 041 历史验收或 045/046 部分结果算成 047 全部通过。
