# 文案、输入和远端适配收尾计划

本轮执行用户授权的剩余工作。交付基于当前 Chromium 153.0.8010.53，固定安装路径和签名身份沿用 061。历史 057 的 75/90、21.33% 输入降幅及异内核性能数据不作为本轮通过证据。

## 步骤与完成标准

1. 按用户补充要求，只同步远端 `e6c17f7df7fd1a403493a2a0d3fb8a3e3093908a` 到 `codex/colleague-sync-20260922`；网络适配和专项验收由同事负责。本次候选继续基于 061，未将同步等同于集成通过。
2. 清理内部状态、风险文案和旁白 → 英文、简体、繁体一致；任务状态只在变化时播报，时间线刷新不重复朗读，不抢用户输入焦点。
3. 优化输入与性能 → 依据实际模型请求削减重复内容，保留证据及授权边界；用代表任务实测，与既有同任务范围比较。性能报告严格区分同内核配置与旧描述性基线。
4. 集中候选构建与验收 → 开发期仅必要定向检查，最后统一运行原生、界面、真实操作及证据检查。失败需修复后验证，不以源码通过代替安装 App 通过。
5. 收敛未关闭条件 → 核查平台设备、发行签名、公证与远端执行条件；可在本机实现的继续完成，缺外部条件的明确列出缺口和所需行动。

## 取舍

- 061 源码与安装包保留为回退点；所有改动在 `codex/closure-20260922` 集中完成。
- 用户调整范围后，TypeSafe 与网络开发线仅同步保留，本轮不编入或测试；验收只使用原本机模型。
- 原生保护阻断、缺设备和未暴露攻击样本分别记录，不调整安全边界来提高分数。
- 不创建公开发行、不推送、不改变既有浏览器用户数据来伪造通过。

最终状态：本轮实现与集中验收记录见 [收尾报告](../audit/closure-2026-09-22.zh-CN.md)。九次代表任务通过；未达标与新增问题按用户要求单列，不开启额外重建。
