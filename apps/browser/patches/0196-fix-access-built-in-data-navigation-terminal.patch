From 5c324b188f75818ad0ba456497e8d4d11d387134 Mon Sep 17 00:00:00 2001
From: Codex <codex@local>
Date: Fri, 25 Sep 2026 08:12:22 +0800
Subject: [PATCH] Preserve built-in data navigation without skipping Access
 network routing

---
 .../browser/aw_content_browser_client.cc        |  3 ++-
 .../browser/aw_content_browser_client.h         |  3 ++-
 chrome/browser/chrome_content_browser_client.cc |  6 ++++--
 chrome/browser/chrome_content_browser_client.h  |  3 ++-
 .../loader/navigation_url_loader_impl.cc        | 17 ++++++++++++++---
 .../browser/loader/navigation_url_loader_impl.h |  3 ++-
 .../browser/loader/url_loader_factory_utils.cc  |  7 +++++--
 .../browser/loader/url_loader_factory_utils.h   |  6 +++++-
 .../prefetch/nav_prefetch_browsertest.cc        |  5 +++--
 .../prefetch_network_context_unittest.cc        |  6 +++---
 .../prefetch/prefetch_test_util_internal.cc     |  2 +-
 .../prefetch/prefetch_test_util_internal.h      |  3 ++-
 .../prefetch_url_loader_interceptor_unittest.cc | 16 ++++++++--------
 .../public/browser/content_browser_client.cc    |  3 ++-
 content/public/browser/content_browser_client.h | 12 +++++++++++-
 .../test/url_loader_interceptor_browsertest.cc  |  3 ++-
 16 files changed, 68 insertions(+), 30 deletions(-)

diff --git a/android_webview/browser/aw_content_browser_client.cc b/android_webview/browser/aw_content_browser_client.cc
index 26268a63e4..1a787813bf 100644
--- a/android_webview/browser/aw_content_browser_client.cc
+++ b/android_webview/browser/aw_content_browser_client.cc
@@ -1166,7 +1166,8 @@ void AwContentBrowserClient::WillCreateURLLoaderFactory(
     bool* bypass_redirect_checks,
     bool* disable_secure_dns,
     network::mojom::URLLoaderFactoryOverridePtr* factory_override,
-    scoped_refptr<base::SequencedTaskRunner> navigation_response_task_runner) {
+    scoped_refptr<base::SequencedTaskRunner> navigation_response_task_runner,
+    NavigationFactorySource navigation_factory_source) {
   TRACE_EVENT0("android_webview",
                "AwContentBrowserClient::WillCreateURLLoaderFactory");
   DCHECK_CURRENTLY_ON(BrowserThread::UI);
diff --git a/android_webview/browser/aw_content_browser_client.h b/android_webview/browser/aw_content_browser_client.h
index 12bf3844b3..0cb5e9f91d 100644
--- a/android_webview/browser/aw_content_browser_client.h
+++ b/android_webview/browser/aw_content_browser_client.h
@@ -244,7 +244,8 @@ class AwContentBrowserClient : public content::ContentBrowserClient {
       bool* bypass_redirect_checks,
       bool* disable_secure_dns,
       network::mojom::URLLoaderFactoryOverridePtr* factory_override,
-      scoped_refptr<base::SequencedTaskRunner> navigation_response_task_runner)
+      scoped_refptr<base::SequencedTaskRunner> navigation_response_task_runner,
+      NavigationFactorySource navigation_factory_source)
       override;
   content::ContentBrowserClient::WebSocketOptions GetWebSocketOptions(
       content::RenderFrameHost* frame) override;
diff --git a/chrome/browser/chrome_content_browser_client.cc b/chrome/browser/chrome_content_browser_client.cc
index 601711d918..7230458c7b 100644
--- a/chrome/browser/chrome_content_browser_client.cc
+++ b/chrome/browser/chrome_content_browser_client.cc
@@ -6783,7 +6783,8 @@ void ChromeContentBrowserClient::WillCreateURLLoaderFactory(
     bool* bypass_redirect_checks,
     bool* disable_secure_dns,
     network::mojom::URLLoaderFactoryOverridePtr* factory_override,
-    scoped_refptr<base::SequencedTaskRunner> navigation_response_task_runner) {
+    scoped_refptr<base::SequencedTaskRunner> navigation_response_task_runner,
+    NavigationFactorySource navigation_factory_source) {
 #if BUILDFLAG(ENABLE_EXTENSIONS_CORE)
   auto* web_request_api =
       extensions::BrowserContextKeyedAPIFactory<extensions::WebRequestAPI>::Get(
@@ -6823,7 +6824,8 @@ void ChromeContentBrowserClient::WillCreateURLLoaderFactory(
       isolation_info, factory_builder);
 
   if (type == URLLoaderFactoryType::kNavigation && frame &&
-      navigation_id.has_value()) {
+      navigation_id.has_value() &&
+      navigation_factory_source != NavigationFactorySource::kBuiltInData) {
     aegis::access::AccessProxyingURLLoaderFactory::MaybeProxyNavigation(
         Profile::FromBrowserContext(browser_context), frame, *navigation_id,
         factory_builder);
diff --git a/chrome/browser/chrome_content_browser_client.h b/chrome/browser/chrome_content_browser_client.h
index f41b3aa856..6bc4284c76 100644
--- a/chrome/browser/chrome_content_browser_client.h
+++ b/chrome/browser/chrome_content_browser_client.h
@@ -728,7 +728,8 @@ class ChromeContentBrowserClient : public content::ContentBrowserClient {
       bool* bypass_redirect_checks,
       bool* disable_secure_dns,
       network::mojom::URLLoaderFactoryOverridePtr* factory_override,
-      scoped_refptr<base::SequencedTaskRunner> navigation_response_task_runner)
+      scoped_refptr<base::SequencedTaskRunner> navigation_response_task_runner,
+      NavigationFactorySource navigation_factory_source)
       override;
   void WillCreateURLLoaderFactoryForDocument(
       content::BrowserContext* browser_context,
diff --git a/content/browser/loader/navigation_url_loader_impl.cc b/content/browser/loader/navigation_url_loader_impl.cc
index dc469222bf..f54b7c9205 100644
--- a/content/browser/loader/navigation_url_loader_impl.cc
+++ b/content/browser/loader/navigation_url_loader_impl.cc
@@ -1272,10 +1272,11 @@ NavigationURLLoaderImpl::CreateNonNetworkLoaderFactory(
   CHECK(frame_tree_node->navigation_request());
 
   // First, check known schemes.
+  bool is_built_in_data_terminal = false;
   if (mojo::PendingRemote<network::mojom::URLLoaderFactory> terminal =
           CreateTerminalNonNetworkLoaderFactory(
               browser_context, storage_partition, frame_tree_node,
-              resource_request.url)) {
+              resource_request.url, &is_built_in_data_terminal)) {
     auto* frame = frame_tree_node->current_frame_host();
 
     // TODO(lukasza, jam): It is unclear why FileURLLoaderFactory is the only
@@ -1302,7 +1303,13 @@ NavigationURLLoaderImpl::CreateNonNetworkLoaderFactory(
                 /*bypass_redirect_checks=*/nullptr,
                 frame_tree_node->navigation_request()->GetNavigationId(),
                 GetUIThreadTaskRunner(
-                    {BrowserTaskType::kNavigationNetworkResponse})),
+                    {BrowserTaskType::kNavigationNetworkResponse}),
+                /*document_context=*/std::nullopt,
+                is_built_in_data_terminal
+                    ? ContentBrowserClient::NavigationFactorySource::
+                          kBuiltInData
+                    : ContentBrowserClient::NavigationFactorySource::
+                          kUnspecified),
             devtools_params));
   }
 
@@ -2188,7 +2195,10 @@ NavigationURLLoaderImpl::CreateTerminalNonNetworkLoaderFactory(
     BrowserContext* browser_context,
     StoragePartitionImpl* storage_partition,
     FrameTreeNode* frame_tree_node,
-    const GURL& url) {
+    const GURL& url,
+    bool* is_built_in_data_terminal) {
+  CHECK(is_built_in_data_terminal);
+  *is_built_in_data_terminal = false;
   // Use `ContentBrowserClient`-supplied factory if non-null, to allow the
   // embedder to override the default factories below.
   if (auto factory_from_client =
@@ -2233,6 +2243,7 @@ NavigationURLLoaderImpl::CreateTerminalNonNetworkLoaderFactory(
     return AboutURLLoaderFactory::Create();
   }
   if (url.GetScheme() == url::kDataScheme) {
+    *is_built_in_data_terminal = true;
     return DataURLLoaderFactory::Create();
   }
 
diff --git a/content/browser/loader/navigation_url_loader_impl.h b/content/browser/loader/navigation_url_loader_impl.h
index 2dbca03dd2..80517e931e 100644
--- a/content/browser/loader/navigation_url_loader_impl.h
+++ b/content/browser/loader/navigation_url_loader_impl.h
@@ -134,7 +134,8 @@ class CONTENT_EXPORT NavigationURLLoaderImpl
   CreateTerminalNonNetworkLoaderFactory(BrowserContext* browser_context,
                                         StoragePartitionImpl* storage_partition,
                                         FrameTreeNode* frame_tree_node,
-                                        const GURL& url);
+                                        const GURL& url,
+                                        bool* is_built_in_data_terminal);
   // Creates a complete URLLoaderFactory for non-network-service-bound requests.
   // Unlike `CreateTerminalNonNetworkLoaderFactory()`, this supports
   // ContentBrowserClient/DevTools interception, external protocols, and unknown
diff --git a/content/browser/loader/url_loader_factory_utils.cc b/content/browser/loader/url_loader_factory_utils.cc
index 2ee5144d0c..d36e6e27ea 100644
--- a/content/browser/loader/url_loader_factory_utils.cc
+++ b/content/browser/loader/url_loader_factory_utils.cc
@@ -151,7 +151,8 @@ ContentClientParams::ContentClientParams(
     std::optional<int64_t> navigation_id,
     scoped_refptr<base::SequencedTaskRunner> navigation_response_task_runner,
     std::optional<ContentBrowserClient::DocumentURLLoaderFactoryContext>
-        document_context)
+        document_context,
+    ContentBrowserClient::NavigationFactorySource navigation_factory_source)
     : browser_context_(browser_context),
       frame_(frame),
       render_process_id_(render_process_id),
@@ -161,6 +162,7 @@ ContentClientParams::ContentClientParams(
       bypass_redirect_checks_(bypass_redirect_checks),
       navigation_id_(navigation_id),
       document_context_(std::move(document_context)),
+      navigation_factory_source_(navigation_factory_source),
       navigation_response_task_runner_(
           std::move(navigation_response_task_runner)) {}
 
@@ -181,7 +183,8 @@ void ContentClientParams::Run(
       request_initiator_.get(), isolation_info_.get(),
       std::move(navigation_id_), std::move(ukm_source_id_), factory_builder,
       header_client, bypass_redirect_checks_.get(), disable_secure_dns,
-      factory_override, std::move(navigation_response_task_runner_));
+      factory_override, std::move(navigation_response_task_runner_),
+      navigation_factory_source_);
   if (type == ContentBrowserClient::URLLoaderFactoryType::kDocumentSubResource &&
       document_context_.has_value()) {
     GetContentClient()->browser()->WillCreateURLLoaderFactoryForDocument(
diff --git a/content/browser/loader/url_loader_factory_utils.h b/content/browser/loader/url_loader_factory_utils.h
index b075bb5e66..1767d9dcb6 100644
--- a/content/browser/loader/url_loader_factory_utils.h
+++ b/content/browser/loader/url_loader_factory_utils.h
@@ -61,7 +61,10 @@ class CONTENT_EXPORT ContentClientParams final {
                           navigation_response_task_runner = nullptr,
                       std::optional<ContentBrowserClient::
                                         DocumentURLLoaderFactoryContext>
-                          document_context = std::nullopt);
+                          document_context = std::nullopt,
+                      ContentBrowserClient::NavigationFactorySource
+                          navigation_factory_source = ContentBrowserClient::
+                              NavigationFactorySource::kUnspecified);
 
   ContentClientParams(const ContentClientParams&) = delete;
   ContentClientParams& operator=(const ContentClientParams&) = delete;
@@ -89,6 +92,7 @@ class CONTENT_EXPORT ContentClientParams final {
   std::optional<int64_t> navigation_id_;
   std::optional<ContentBrowserClient::DocumentURLLoaderFactoryContext>
       document_context_;
+  ContentBrowserClient::NavigationFactorySource navigation_factory_source_;
   scoped_refptr<base::SequencedTaskRunner> navigation_response_task_runner_;
 };
 
diff --git a/content/browser/preloading/prefetch/nav_prefetch_browsertest.cc b/content/browser/preloading/prefetch/nav_prefetch_browsertest.cc
index fc6f116279..66f206ebe2 100644
--- a/content/browser/preloading/prefetch/nav_prefetch_browsertest.cc
+++ b/content/browser/preloading/prefetch/nav_prefetch_browsertest.cc
@@ -1458,7 +1458,8 @@ class PreloadActivationReportInterceptionBrowserClient
       bool* bypass_redirect_checks,
       bool* disable_secure_dns,
       network::mojom::URLLoaderFactoryOverridePtr* factory_override,
-      scoped_refptr<base::SequencedTaskRunner> navigation_response_task_runner)
+      scoped_refptr<base::SequencedTaskRunner> navigation_response_task_runner,
+      NavigationFactorySource navigation_factory_source)
       override {
     if (type == URLLoaderFactoryType::kDocumentSubResource &&
         factory_override) {
@@ -1485,7 +1486,7 @@ class PreloadActivationReportInterceptionBrowserClient
         isolation_info, std::move(navigation_id), ukm_source_id,
         factory_builder, header_client, bypass_redirect_checks,
         disable_secure_dns, factory_override,
-        std::move(navigation_response_task_runner));
+        std::move(navigation_response_task_runner), navigation_factory_source);
   }
 
   void OnRequest(const GURL& url) { intercepted_urls_.push_back(url); }
diff --git a/content/browser/preloading/prefetch/prefetch_network_context_unittest.cc b/content/browser/preloading/prefetch/prefetch_network_context_unittest.cc
index 193379ba0e..ce9226b5a5 100644
--- a/content/browser/preloading/prefetch/prefetch_network_context_unittest.cc
+++ b/content/browser/preloading/prefetch/prefetch_network_context_unittest.cc
@@ -75,7 +75,7 @@ TEST_F(PrefetchNetworkContextTest, CreateIsolatedURLLoaderFactory) {
                   testing::Eq(std::nullopt),
                   ukm::SourceIdObj::FromInt64(main_rfh()->GetPageUkmSourceId()),
                   testing::_, testing::NotNull(), testing::NotNull(),
-                  testing::IsNull(), testing::IsNull(), testing::IsNull()));
+                  testing::IsNull(), testing::IsNull(), testing::IsNull(), testing::_));
 
   // Unused fields are marked as `{}`.
   auto prefetch_request = PrefetchRequest::CreateRendererInitiated(
@@ -113,7 +113,7 @@ TEST_F(PrefetchNetworkContextTest,
                   testing::Eq(std::nullopt),
                   ukm::SourceIdObj::FromInt64(main_rfh()->GetPageUkmSourceId()),
                   testing::_, testing::NotNull(), testing::NotNull(),
-                  testing::IsNull(), testing::IsNull(), testing::IsNull()));
+                  testing::IsNull(), testing::IsNull(), testing::IsNull(), testing::_));
 
   // Unused fields are marked as `{}`.
   auto prefetch_request = PrefetchRequest::CreateRendererInitiated(
@@ -149,7 +149,7 @@ TEST_F(PrefetchNetworkContextTest,
           IsSameOriginWith(kReferringUrl), IsEmptyIsolationInfo(),
           testing::Eq(std::nullopt), testing::Eq(ukm::kInvalidSourceIdObj),
           testing::_, testing::NotNull(), testing::NotNull(), testing::IsNull(),
-          testing::IsNull(), testing::IsNull()));
+          testing::IsNull(), testing::IsNull(), testing::_));
 
   // Unused fields are marked as `{}`.
   auto prefetch_request =
diff --git a/content/browser/preloading/prefetch/prefetch_test_util_internal.cc b/content/browser/preloading/prefetch/prefetch_test_util_internal.cc
index 7c047e235b..65d2e8e08e 100644
--- a/content/browser/preloading/prefetch/prefetch_test_util_internal.cc
+++ b/content/browser/preloading/prefetch/prefetch_test_util_internal.cc
@@ -534,7 +534,7 @@ ScopedMockContentBrowserClient::ScopedMockContentBrowserClient() {
       WillCreateURLLoaderFactory(
           _, _, _,
           ContentBrowserClient::URLLoaderFactoryType::kDocumentSubResource, _,
-          _, _, _, _, _, _, _, _, _))
+          _, _, _, _, _, _, _, _, _, _))
       .Times(::testing::AnyNumber());
 }
 
diff --git a/content/browser/preloading/prefetch/prefetch_test_util_internal.h b/content/browser/preloading/prefetch/prefetch_test_util_internal.h
index e4bc87d54b..dc958b53a9 100644
--- a/content/browser/preloading/prefetch/prefetch_test_util_internal.h
+++ b/content/browser/preloading/prefetch/prefetch_test_util_internal.h
@@ -200,7 +200,8 @@ class ScopedMockContentBrowserClient : public TestContentBrowserClient {
        bool* disable_secure_dns,
        network::mojom::URLLoaderFactoryOverridePtr* factory_override,
        scoped_refptr<base::SequencedTaskRunner>
-           navigation_response_task_runner),
+           navigation_response_task_runner,
+       NavigationFactorySource navigation_factory_source),
       (override));
 
  private:
diff --git a/content/browser/preloading/prefetch/prefetch_url_loader_interceptor_unittest.cc b/content/browser/preloading/prefetch/prefetch_url_loader_interceptor_unittest.cc
index bd10a68d50..200025a86b 100644
--- a/content/browser/preloading/prefetch/prefetch_url_loader_interceptor_unittest.cc
+++ b/content/browser/preloading/prefetch/prefetch_url_loader_interceptor_unittest.cc
@@ -387,7 +387,7 @@ class PrefetchURLLoaderInterceptorTestBase : public PrefetchingMetricsTestBase {
         *test_content_browser_client(),
         WillCreateURLLoaderFactory(
             _, _, _, ContentBrowserClient::URLLoaderFactoryType::kNavigation, _,
-            _, _, _, _, _, _, _, _, _))
+            _, _, _, _, _, _, _, _, _, testing::_))
         .Times(::testing::AtMost(1));
   }
 
@@ -431,7 +431,7 @@ TEST_F(PrefetchURLLoaderInterceptorTest,
                   ukm::SourceIdObj::FromInt64(
                       navigation_request()->GetNextPageUkmSourceId()),
                   testing::_, testing::IsNull(), testing::NotNull(),
-                  testing::IsNull(), testing::IsNull(), testing::IsNull()));
+                  testing::IsNull(), testing::IsNull(), testing::IsNull(), testing::_));
 
   auto prefetch_container = CreateSpeculationRulesPrefetchContainer(
       kTestUrl, PrefetchType(PreloadingTriggerType::kSpeculationRule,
@@ -483,7 +483,7 @@ TEST_F(PrefetchURLLoaderInterceptorTest,
                   ukm::SourceIdObj::FromInt64(
                       navigation_request()->GetNextPageUkmSourceId()),
                   testing::_, testing::IsNull(), testing::NotNull(),
-                  testing::IsNull(), testing::IsNull(), testing::IsNull()));
+                  testing::IsNull(), testing::IsNull(), testing::IsNull(), testing::_));
 
   auto prefetch_container = CreateSpeculationRulesPrefetchContainer(
       kTestUrl, PrefetchType(PreloadingTriggerType::kSpeculationRule,
@@ -544,7 +544,7 @@ TEST_F(PrefetchURLLoaderInterceptorTest,
                   ukm::SourceIdObj::FromInt64(
                       navigation_request()->GetNextPageUkmSourceId()),
                   testing::_, testing::IsNull(), testing::NotNull(),
-                  testing::IsNull(), testing::IsNull(), testing::IsNull()));
+                  testing::IsNull(), testing::IsNull(), testing::IsNull(), testing::_));
 
   // No cookies are copied for prefetches where |use_isolated_network_context|
   // is false (i.e. same origin prefetches).
@@ -592,7 +592,7 @@ TEST_F(PrefetchURLLoaderInterceptorTest,
                   ukm::SourceIdObj::FromInt64(
                       navigation_request()->GetNextPageUkmSourceId()),
                   testing::_, testing::IsNull(), testing::NotNull(),
-                  testing::IsNull(), testing::IsNull(), testing::IsNull()));
+                  testing::IsNull(), testing::IsNull(), testing::IsNull(), testing::_));
 
   // Creates a same-origin embedder prefetch, which means cookie copy is not
   // needed.
@@ -868,7 +868,7 @@ TEST_F(PrefetchURLLoaderInterceptorTest, DISABLE_ASAN(ProbeSuccess)) {
                   ukm::SourceIdObj::FromInt64(
                       navigation_request()->GetNextPageUkmSourceId()),
                   testing::_, testing::IsNull(), testing::NotNull(),
-                  testing::IsNull(), testing::IsNull(), testing::IsNull()));
+                  testing::IsNull(), testing::IsNull(), testing::IsNull(), testing::_));
 
   auto prefetch_container = CreateSpeculationRulesPrefetchContainer(
       kTestUrl, PrefetchType(PreloadingTriggerType::kSpeculationRule,
@@ -1121,7 +1121,7 @@ TEST_F(PrefetchURLLoaderInterceptorTest, DISABLE_ASAN(HandleRedirects)) {
                   ukm::SourceIdObj::FromInt64(
                       navigation_request()->GetNextPageUkmSourceId()),
                   testing::_, testing::IsNull(), testing::NotNull(),
-                  testing::IsNull(), testing::IsNull(), testing::IsNull()))
+                  testing::IsNull(), testing::IsNull(), testing::IsNull(), testing::_))
       .Times(2);
 
   auto prefetch_container = CreateSpeculationRulesPrefetchContainer(
@@ -1193,7 +1193,7 @@ TEST_F(PrefetchURLLoaderInterceptorTest,
                   ukm::SourceIdObj::FromInt64(
                       navigation_request()->GetNextPageUkmSourceId()),
                   testing::_, testing::IsNull(), testing::NotNull(),
-                  testing::IsNull(), testing::IsNull(), testing::IsNull()))
+                  testing::IsNull(), testing::IsNull(), testing::IsNull(), testing::_))
       .Times(2);
 
   auto prefetch_container = CreateSpeculationRulesPrefetchContainer(
diff --git a/content/public/browser/content_browser_client.cc b/content/public/browser/content_browser_client.cc
index fb6b7af495..d4cb402809 100644
--- a/content/public/browser/content_browser_client.cc
+++ b/content/public/browser/content_browser_client.cc
@@ -1156,7 +1156,8 @@ void ContentBrowserClient::WillCreateURLLoaderFactory(
     bool* bypass_redirect_checks,
     bool* disable_secure_dns,
     network::mojom::URLLoaderFactoryOverridePtr* factory_override,
-    scoped_refptr<base::SequencedTaskRunner> navigation_response_task_runner) {
+    scoped_refptr<base::SequencedTaskRunner> navigation_response_task_runner,
+    NavigationFactorySource navigation_factory_source) {
   DCHECK(browser_context);
 }
 
diff --git a/content/public/browser/content_browser_client.h b/content/public/browser/content_browser_client.h
index 312ba6850b..7f3c9cd428 100644
--- a/content/public/browser/content_browser_client.h
+++ b/content/public/browser/content_browser_client.h
@@ -2024,6 +2024,14 @@ class CONTENT_EXPORT ContentBrowserClient {
     kEarlyHints,
   };
 
+  // Trusted provenance for a navigation factory's terminal. The default
+  // includes Network Service, prefetch cache, and custom factories; only the
+  // built-in data: terminal has a distinct value.
+  enum class NavigationFactorySource {
+    kUnspecified,
+    kBuiltInData,
+  };
+
   // Browser-owned identity of a document factory. The URL is taken from the
   // pending NavigationRequest or the last committed document, never from the
   // renderer's request initiator (which may be opaque for sandboxed pages).
@@ -2142,7 +2150,9 @@ class CONTENT_EXPORT ContentBrowserClient {
       bool* bypass_redirect_checks,
       bool* disable_secure_dns,
       network::mojom::URLLoaderFactoryOverridePtr* factory_override,
-      scoped_refptr<base::SequencedTaskRunner> navigation_response_task_runner);
+      scoped_refptr<base::SequencedTaskRunner> navigation_response_task_runner,
+      NavigationFactorySource navigation_factory_source =
+          NavigationFactorySource::kUnspecified);
 
   // Called only for document subresource factories with trusted document
   // identity, after WillCreateURLLoaderFactory().
diff --git a/content/test/url_loader_interceptor_browsertest.cc b/content/test/url_loader_interceptor_browsertest.cc
index 0c3ed732cc..eb1c388a1c 100644
--- a/content/test/url_loader_interceptor_browsertest.cc
+++ b/content/test/url_loader_interceptor_browsertest.cc
@@ -175,7 +175,8 @@ class TestBrowserClientWithHeaderClient
       bool* bypass_redirect_checks,
       bool* disable_secure_dns,
       network::mojom::URLLoaderFactoryOverridePtr* factory_override,
-      scoped_refptr<base::SequencedTaskRunner> navigation_response_task_runner)
+      scoped_refptr<base::SequencedTaskRunner> navigation_response_task_runner,
+      NavigationFactorySource navigation_factory_source)
       override {
     if (header_client) {
       receivers_.Add(this, header_client->InitWithNewPipeAndPassReceiver());
-- 
2.54.0 (Apple Git-157)

